# Services

This directory contains API service files for making HTTP requests to your backend.

## Structure

- `api.ts` - Base API configuration (axios/fetch setup)
- `authService.ts` - Authentication API calls (login, register, logout)
- `rideService.ts` - Ride-related API calls (get rides, book ride, etc.)
- `userService.ts` - User profile API calls

