# Contexts

This directory contains React Context providers for global state management.

## Available Contexts

- `AuthContext.tsx` - Authentication state (user, token, login/logout)
- `UserContext.tsx` - User profile information
- `ThemeContext.tsx` - Theme management (light/dark mode)

