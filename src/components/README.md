# Components

This directory contains reusable UI components.

## Example Components

- `Button/` - Custom button component
- `Input/` - Custom input component
- `Card/` - Card container component
- `RideCard/` - Ride listing card component
- `Header/` - App header component

